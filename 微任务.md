# 微任务

## 种类

### 浏览器中的微任务

- Promise 方法
  - Promise.then
  - Promise.catch
  - Promise.finally
- queueMicrotask
  - 将一个函数添加到微任务队列中

  ```js
  console.log(1);
  queueMicrotask(() => {
    console.log("queueMicrotask 2");
  });
  console.log(3);
  // 输出 1 3 queueMicrotask 2
  ```

- MutationObserver
  - 监听 DOM 节点的变化

  ```js
  // 新建一个 MutationObserver 实例
  // const observer = new MutationObserver(callback);
  const observer = new MutationObserver((mutationsList, observer) => {
    for (const mutation of mutationsList) {
      console.log("类型:", mutation.type);
      console.log("发生变化的节点:", mutation.target);
      console.log("新增:", mutation.addedNodes);
      console.log("删除:", mutation.removedNodes);
    }
  });
  observer.observe(document.body, {
    childList: true,
    attributes: true,
    characterData: true,
    subtree: true,
  });

  onUnmounted(() => {
    observer.disconnect();
  });
  ```

### Node.js 中的微任务

- process.nextTick
- queueMicrotask
- MutationObserver
