Review 代码
AI 写代码后，review 的核心不是「挑语法错」，而是防止"看起来能跑、实际不懂业务/不安全/不可维护"的代码进主干。别问"像不像对"，要问"有没有证据证明它对、安全、可维护"。
AI 最容易踩的坑（举例）：
- 业务规则想当然：让 AI"优化登录逻辑"，它可能顺手绕过了风控校验
- 只写 happy path：漏掉异常、空值、并发、权限失败等分支
- 测试配合实现：先写实现再补一个刚好能过的测试，而不是从需求出发验证
- 静默改语义：把 && 改成 ||、把强校验改成弱校验，diff 很小风险很大
- 安全别指望 AI 自查：注入、越权、密钥写日志等问题 AI review 工具经常漏
推荐做法：
1. 机器先挡低级问题：lint、type check、单测、SAST、依赖/密钥扫描
2. 人工重点看三层：这个改动该不该做 → 是否符合架构 → 每处分支/错误处理是否经得起推敲
3. 看不懂就打回、没测试证据就打回、安全边界说不清就打回
AI 负责提速度，人负责保证系统长期正确——好 reviewer 越来越值钱，不是因为能挑格式问题，而是能判断这个改动半年后还有没有人敢维护。