import { createGenericH5NovelAdapter } from "../genericH5NovelAdapter.js";

export const unlimitedNovelsAdapter = createGenericH5NovelAdapter({
  id: "unlimited-novels",
  hosts: ["unlimitednovels.com"],
});
